"""Optional Streamlit artifact browser.

Launch with ``streamlit run src/regime/app/main.py -- --root experiments`` after
installing the ``app`` dependency group. Importing :mod:`regime` never imports
Streamlit.
"""
