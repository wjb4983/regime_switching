# 5. Fit the clustering baseline.
model: kmeans
input: artifacts/features/core_market.parquet
output: artifacts/models/kmeans_regime
features: [return_1d, realized_volatility_21d]
n_states: 2
scale: true
n_init: 10
random_seed: 42
